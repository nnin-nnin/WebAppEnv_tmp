users:
  - username: admin
    password: "123456"
    type: admin
    enabled: true
    purpose: "Initial administrator account"
    creation: "Seeded directly in the database via initial database seed (simulating the web installer step 3)."
